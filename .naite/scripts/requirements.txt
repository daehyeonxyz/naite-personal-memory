# Optional analysis-tool dependencies for .naite/scripts/ (forest-* diagnostics only).
# Most scripts (build-*, lint-*, sync-agents) use only the standard library.
#
#   forest-communities.py          : networkx
#   forest-assign.py               : numpy
#   forest-retrieval-experiment.py : numpy, networkx, scikit-learn
#   forest-dashboard.py            : standard library only (no dependency)
#
# networkx>=3.0 is required: forest-communities.py and forest-retrieval-experiment.py
# use networkx.algorithms.community.louvain_communities, added in networkx 3.0.
#
# Install with:  pip install -r .naite/scripts/requirements.txt
networkx>=3.0
numpy>=1.24
scikit-learn>=1.3
