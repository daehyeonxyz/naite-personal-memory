# Trunk

naite 의 큐레이션된 진입점, 나무의 줄기다. 모든 페이지를 나열하지 않는다. 도메인 hub 와 branch 메타 페이지만 노출하고, 나머지는 drill-down 으로 발견한다 (`docs/CONVENTIONS.md § trunk.md discipline`).

처음에는 비어 있는 것이 정상이다. 잎이 쌓이면 `/naite care --check` 가 도메인 노출 임계 통과를 surface 하고, 사용자 확인 후 섹션이 자란다.

## Knowledge domains

(아직 노출 임계를 통과한 도메인이 없다.)

## Branches

(아직 자라기 시작한 가지가 없다. 처음이라면 `/naite start` 로 다른 AI 의 기록을 가져와 첫 나무를 짓는다. 이미 익숙하면 `/naite grow` 로 자료를 바로 심는다.)
